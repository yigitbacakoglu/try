<% if flash.any? %>
    <% flash.each do |key, value| %>
        <div class="alert <%= key %> fade in">
          <button type="button" class="close" data-dismiss="alert">×</button>
          <i class="icon-bullhorn"></i> <strong> <%= value %> </strong>
        </div>

    <% end %>
    <% flash.sweep %>
<% end %>